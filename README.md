# ML_serving_project
## Problem Serve a trained ML model as a production-ready API.  ## Solution Implemented training pipeline with MLflow and inference service using FastAPI.  ## How to run docker build -t ml-serving . docker run -p 8000:8000 ml-serving  ## API POST /predict
